# eBird API key — get one free at https://ebird.org/api/keygen
EBIRD_API_KEY=
# Location for nearby sightings (same coordinates as the Weather service)
LATITUDE=
LONGITUDE=
# Optional: search radius in km around your location (default 15)
# BIRDS_RADIUS_KM=15
