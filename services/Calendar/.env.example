# Create a project at https://console.cloud.google.com
# Enable the Google Calendar API and create OAuth 2.0 credentials (Web application)
# Add the redirect URI below to Authorized redirect URIs
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost/calendar/google/callback
